exported funky main() {
	
	com myValue int = 6;

	myValue += 2;
	myValue += 4;
	myValue += 8;

	block LoopdiLoop {

		myValue += 1;

		if COMPARE.IsLessThan(myValue, 100) { goto LoopdiLoop; };
	};

	return myValue;
}